/* Nebula Dominion — shared mock constants for ND screens
 *
 * Placeholder values used while the screens are wired to design but not yet
 * to backend data. Centralized so that swapping in real population/economy
 * data is a single-file edit rather than a multi-screen sweep.
 */

export const POP_USED = 180;
export const POP_MAX = 240;
